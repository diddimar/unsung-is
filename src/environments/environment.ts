// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: 'AIzaSyCBpOzZaknxHgmrsyCRQ7p5Vsuh2vY7dPE',
    authDomain: 'unsung-is.firebaseapp.com',
    databaseURL: 'https://unsung-is.firebaseio.com',
    projectId: 'unsung-is',
    storageBucket: 'unsung-is.appspot.com',
    messagingSenderId: '545026911432',
    appId: '1:545026911432:web:1e63d9553ca384d0'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
